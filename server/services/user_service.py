# Business logic to handle user data (CRUD operations, validations, etc.)

def create_user(name: str):
    # In a real project, you might interact with the database:
    # user = User(name=name)
    # db.session.add(user)
    # db.session.commit()
    # return user
    pass

def get_all_users():
    # Example logic to fetch users from the database
    # return User.query.all()
    pass
